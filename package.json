{
  "name": "webserver",
  "version": "1.0.0",
  "description": "Initial webserver to handle Raspberry Pi",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonathan Cameron",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
